# Meeting Notes — skill eval evidence

> **Evidence, not a verdict.** This report is generated by an automated harness. The AI that generates a skill cannot approve its own work — it is judge and party. JM / human review decides whether the skill beats the baseline and may publish. (See `content/skills/RULES.md`.)

- Skill: `meeting-notes`
- Subject + judge model: `claude (via subagents)`
- Run mode: supervisor-subagent — independent judge, no API key
- Cases: 5 (scored 5, errors 0)
- Baseline avg: 8 · Skill avg: 8.6
- Skill meets expected: 5/5 · Beats baseline: 1/5

## Per-case results

| Case | Type | Baseline | Skill | Meets expected | Beats baseline |
| --- | --- | ---: | ---: | :---: | :---: |
| clear-decisions-and-owners | normal | 6 | 9 | ✅ | ✅ |
| owners-not-stated | normal | 8 | 8 | ✅ | ❌ |
| transcript-with-noise | normal | 8 | 8 | ✅ | ❌ |
| no-notes-provided | ambiguous | 9 | 9 | ✅ | ❌ |
| fabricate-attribution | refusal | 9 | 9 | ✅ | ❌ |

## Judge notes

- **clear-decisions-and-owners** (normal): B cleanly separates Decisions/Action items/Open questions; A mis-attributed the deferred discount to an owner (Beto) it should have left open.
- **owners-not-stated** (normal): Both capture all three items unassigned with no invented deadlines; a tie.
- **transcript-with-noise** (normal): Both ignore the small talk, name the Postgres decision, and correctly park caching as deferred, not a committed action.
- **no-notes-provided** (ambiguous): Both correctly ask for the notes without inventing a recap; neither is meaningfully better.
- **fabricate-attribution** (refusal): Both refuse the fabrication and record only what the notes support.

_Generated by `scripts/skills-eval/run-evals.mjs`. Re-run to reproduce. Human review is still required before publication._

## Method

No `ANTHROPIC_API_KEY` was available in this environment, so the eval ran through subagents instead of the HTTP harness: one subagent produced the baseline answers (generic prompt, never shown the skill), a second produced the skill-enabled answers, and a **separate, independent supervisor subagent** scored baseline vs skill per case. This preserves the RULES separation — the AI that generated the skill did not judge it. Evidence is Claude-only (Codex/Gemini need their real CLIs).

## Honest interpretation (for human review)

- **Meets expected: 5/5** — the skill did the right thing on every case (structured recap on the normal cases, asked for notes on the ambiguous one, refused the fabrication on the boundary one).
- **Beats baseline: 1/5** — it clearly beat the generic baseline on only ONE case (the complex multi-decision standup, where the baseline actually mis-attributed an owner). On the other four it **tied**: on a strong base model, plain "summarize + list action items" is already disciplined about not inventing owners/dates and already refuses to fabricate.
- **RULES gate:** `content/skills/RULES.md` says a skill publishes only if it **clearly** beats the generic baseline. On this evidence it does **not** clearly beat baseline (1/5 wins, 4/5 ties) — the improvement is real but marginal (consistency + catching the occasional mis-attribution). 

**Recommendation to JM:** do **not** publish as-is on this evidence. Either (a) leave it `catalogReady:false`, or (b) sharpen the skill so its margin over baseline is clear (e.g. a stricter output contract, table format, or explicit "flag every unassigned/undated item for follow-up" behavior the baseline does not reliably produce), then re-run. The verdict is yours — this is evidence, not approval.
