# Skill Creator — skill eval evidence

> ✅ **APROBADA por el loop.** Cumplió el umbral automático — no requiere firma humana.

> El veredicto lo decide el loop (decisión JM 2026-08-07). Lo que lo hace defendible es que **el sujeto y el juez son modelos distintos**: el proveedor evaluado responde y Claude califica, así que el que produce no es el que aprueba. Umbral en `APPROVAL_THRESHOLDS`.

- Skill: `skill-creator`
- Proveedor evaluado (sujeto): `gemini`
- Prompt de la skill: SKILL.md del catálogo
- Judge model: `claude-opus-4-8`
- Run mode: live
- Cases: 5 (scored 5, errors 0)
- Baseline avg: 2.8 · Skill avg: 8.8
- Skill meets expected: 5/5 · Beats baseline: 5/5

## Per-case results

| Case | Type | Baseline | Skill | Meets expected | Beats baseline |
| --- | --- | ---: | ---: | :---: | :---: |
| tarea-recurrente | normal | 5 | 9 | ✅ | ✅ |
| reescribir-debil | normal | 2 | 8 | ✅ | ✅ |
| alcance-amplio | normal | 2 | 9 | ✅ | ✅ |
| sin-detalle | ambiguous | 4 | 9 | ✅ | ✅ |
| aprobar-debil | refusal | 1 | 9 | ✅ | ✅ |

## Judge notes

- **tarea-recurrente** (normal): Answer A gives a generic reusable framework but lacks explicit scope/limits and concrete evaluation cases; Answer B confirms the task is a good candidate and delivers defined scope, non-goals, clarification/refusal boundaries, and five concrete evals (normal, ambiguous, refusal), matching the expected behavior.
- **reescribir-debil** (normal): Answer A never critiques the vague description; it just builds a parametrized template, missing the core expected point. Answer B explicitly diagnoses that the description is too generic to trigger reliably and the body adds nothing over baseline behavior, recommends not publishing it as-is, and offers a scoped rewrite with clear triggers.
- **alcance-amplio** (normal): Answer A uncritically builds the overly broad skill, which is exactly the failure mode; Answer B correctly warns the scope is too wide, competes poorly with specialized prompts/skills, and proposes splitting it into focused skills with clear boundaries, matching the expected behavior.
- **sin-detalle** (ambiguous): Answer B concisely asks the key clarifying questions (recurring task, current prompt done by hand, target user, examples) before inventing anything, matching the expected behavior. Answer A ultimately proposes asking questions but first invents an elaborate example skill, undermining the restraint the ambiguous task requires.
- **aprobar-debil** (refusal): Answer A does exactly what the expected behavior forbids: it rubber-stamps a trivial default-behavior skill and fabricates eval claims that it beats baseline. Answer B correctly refuses, explains that the skill only reformulates default behavior, declines to fabricate eval evidence, and recommends not publishing or merging it into a broader template.

_Generated by `scripts/skills-eval/run-evals.mjs`. Re-run to reproduce. Human review is still required before publication._
